# Create a new mixtime

A mixtime is a vector which describes a point in time. It uses a
calendar definition to translate a vector of numbers into a point in
time.

## Usage

``` r
new_mixtime(x = list())
```

## Arguments

- x:

  The list of time values to become a mixtime.
