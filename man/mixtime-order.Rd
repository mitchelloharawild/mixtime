% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{mixtime-order}
\alias{mixtime-order}
\alias{xtfrm.mixtime}
\alias{sort.mixtime}
\alias{xtfrm,mixtime::mixtime-method}
\alias{sort,mixtime::mixtime-method}
\title{Ordering of mixtime vectors}
\usage{
## S7 method for class <mixtime::mixtime>
xtfrm(x)

## S7 method for class <mixtime::mixtime>
sort(x, decreasing = FALSE, na.last = NA, ..., align_discrete = 0.5)
}
\arguments{
\item{x, decreasing, na.last, ...}{See \code{\link[=sort]{sort()}}.}

\item{align_discrete}{The fractional position (\code{0} = start, \code{1} = end)
within a discrete time span's chronon to use as its ordering instant
when reconciling it against a value of another chronon. Default \code{0.5}
(the midpoint).}
}
\value{
\code{sort()} returns a mixtime vector; \code{xtfrm()} returns a numeric vector
suitable for ranking.
}
\description{
\code{sort()} and \code{xtfrm()} (which in turn powers \code{order()}, \code{rank()}, and
comparisons like \code{min()}/\code{max()}) order a mixtime vector chronologically.
Continuous time points (e.g. \code{POSIXct}-backed values) order by their exact
instant, but a \emph{discrete} time value (e.g. \code{yearmonth(2020, 1)}) represents
an entire span of time rather than a single instant, so ordering it
against a value of another granularity (or against a continuous value)
first requires picking the instant \emph{within} that span to compare from.

\code{align_discrete} controls that choice: \code{0} anchors discrete values to the
start of their span, \code{1} to the end, and the default \code{0.5} to the
midpoint - so, for example, \code{yearmonth(2020, 1)} sorts before, at the same
position as, or after \code{date("2020-01-15")} according to whether
\code{align_discrete} is closer to \code{0}, exactly \code{0.5}, or closer to \code{1}. This
only affects relative order between differing chronons/granularities;
values that already share a chronon keep their exact relative order for
any \code{align_discrete}, since shifting every value by the same amount can't
change their order.

Because base R's \code{xtfrm()} generic takes no arguments beyond \code{x},
\code{align_discrete} can only be customised through \code{sort()}; \code{xtfrm()} (and
anything built on it, such as \code{order()} or \code{rank()}) always aligns to the
midpoint.
}
\examples{
x <- c(yearmonth(2020, 1), date("2020-01-15"), yearmonth(2020, 2))
sort(x)
sort(x, align_discrete = 0)   # yearmonth(2020, 1) now sorts after the 15th
sort(x, decreasing = TRUE)

}
