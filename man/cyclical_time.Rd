% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_cyclical_time.R
\name{cyclical_time}
\alias{cyclical_time}
\title{Cyclical time representation}
\usage{
cyclical_time(chronon, cycle)
}
\arguments{
\item{chronon}{A time unit object representing the chronon (e.g., \code{tu_month(1L)})}

\item{cycle}{A time unit object representing the cycle (e.g., \code{tu_year(1L)})}
}
\value{
An function used to create cyclical time points.
}
\description{
\code{cyclical_time()} creates a cyclical time representation using specified
cycle and chronon (and optionally display granules). The cycles is the
larger time unit that defines the time period over which the chronon loops
over (e.g. a week, \code{tu_week(1L)}). The chronon is the smaller time unit that
iterates within each cycle (e.g. a day, \code{tu_day(1L)}). Combined, these two
granules form a cyclical time point (e.g. day of the week).
}
\examples{

day_of_week <- cyclical_time(tu_day(1L), tu_week(1L))
day_of_week(Sys.Date())

month_of_year <- cyclical_time(tu_month(1L), tu_year(1L))
month_of_year(Sys.Date())

}
