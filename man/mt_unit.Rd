% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00_classes.R, R/cal0_time_civil.R,
%   R/cal0_time_solar.R
\name{mt_unit}
\alias{mt_unit}
\alias{mt_tz_unit}
\alias{mt_loc_unit}
\title{Base S7 class for creating new time units}
\usage{
mt_unit(.data = 1L)

mt_tz_unit(.data = 1L, tz = "UTC")

mt_loc_unit(.data = 1L, lat = 0, lon = 0, alt = 0)
}
\arguments{
\item{.data}{The number of time units}

\item{tz}{The timezone name for the unit (valid units can be found with \verb{[tzdb::tzdb_names()]})}
}
\value{
A time unit object of class \code{mt_unit}
}
\description{
This class is the primative class for time units, and should
be extended from when creating new time units. A new class
is typically created with S7 using:
\code{S7::new_class("tu_***", parent = mt_tz_unit)}
}
\details{
Time units are the building blocks of calendars in mixtime. Each unit
represents a specific temporal component (e.g., day, month, year) and
can be combined using \code{\link[=new_calendar]{new_calendar()}} to create a calendar system.

When creating custom calendars, define time unit classes that inherit
from either \code{mt_unit} (for standard units) or \code{mt_tz_unit} (for
timezone-aware units), then pass them as named arguments to
\code{\link[=new_calendar]{new_calendar()}}. The calendar will use these names to create
constructor functions accessible via \code{$} notation (e.g., \code{calendar$day(1L)}).
}
\section{Calendar Algebra Methods}{


Time units enable calendar arithmetic through two key generic methods
that should be implemented for custom time units:
\itemize{
\item \code{chronon_cardinality(from, to, at)} - Returns the number of \code{to} units
that fit within one \code{from} unit. This can be a fixed value (e.g.,
7 days per week) or variable based on \code{at} (e.g., 28-31 days per month).
\item \code{chronon_divmod(x, from, to)} - Converts time unit \code{x} from units of
\code{from} to units of \code{to}, returning a list with \code{chronon} (the quotient)
and \code{remainder}. This enables conversions between units that have
variable cardinality (e.g., the date 2020-03-23 to the month 2020-03).
All conversions should be based on chronons since epoch (1970-01-01),
in the UTC time zone.
}

These methods work together to enable mixtime to perform calendar-aware
arithmetic, understanding that months have variable lengths and handling
timezone-aware conversions.
}

\examples{
# Create a timezone-aware unit class

# Use these units to create a calendar
my_calendar <- new_calendar(
  day = S7::new_class("tu_my_day", parent = mt_unit),
  month = S7::new_class("tu_my_month", parent = mt_tz_unit),
  class = "my_calendar"
)

# Access unit constructors from the calendar
my_calendar$day(1L)
my_calendar$month(3L, tz = "America/New_York")

}
\seealso{
\code{\link[=new_calendar]{new_calendar()}} for creating calendars from time units
}
